version: '3'
services:
  llm-api-cache-proxy:
    image: nginx:1.23.4
    container_name: llm-api-cache-proxy
    ports:
      - 127.0.0.1:81:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./cache.conf:/etc/nginx/cache.conf:ro
      - ./server_cache:/server_cache:rw